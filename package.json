{
  "name": "accord-agent-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Starter multi-agent drafting pipeline for Accord Project templates",
  "scripts": {
    "dev": "tsx src/cli/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli/index.js",
    "test": "tsx tests/run-tests.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}